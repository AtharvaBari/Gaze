name: Gaze
packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle.git
    from: 2.5.0

targets:
  Gaze:
    type: application
    platform: macOS
    deploymentTarget: 14.0
    sources:
      - path: Gaze
        excludes:
          - Tests
    info:
      path: Gaze/Info.plist
      properties:
        LSUIElement: true
        CFBundleName: Gaze
        CFBundleDisplayName: Gaze
        CFBundleIdentifier: com.gaze.Gaze
        CFBundleVersion: "5"
        CFBundleShortVersionString: "1.2.1"
        CFBundleExecutable: $(EXECUTABLE_NAME)
        CFBundlePackageType: $(PRODUCT_BUNDLE_PACKAGE_TYPE)
        SUFeedURL: "https://AtharvaBari.github.io/Gaze-MacOS/appcast.xml"
        SUEnableAutomaticChecks: true
        SUPublicEDKey: "rIBVwBzLoQwef7fvTQZEsWXSvFDUK46zI6qji9GG95g="
        NSMicrophoneUsageDescription: "Gaze listens to your microphone only while you hold the Fn + ⌘ push-to-talk hotkey."
        NSSpeechRecognitionUsageDescription: "Gaze uses on-device speech recognition to transcribe what you say while Fn + ⌘ is held."
    dependencies:
      - package: Sparkle
    settings:
      base:
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: NO
